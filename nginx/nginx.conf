upstream kensens {
    server app_portfolio_website:8000;
}

server {
    listen 80;
    server_name localhost 127.0.0.1;
    return 301 https://localhost$request_uri;
}

server {
    listen 443 ssl;
    server_name localhost 127.0.0.1;
    server_tokens off;
    client_max_body_size 15M;
    ssl_certificate /etc/nginx/ssl/cert.pem;
    ssl_certificate_key /etc/nginx/ssl/key.pem;

    location / {
        proxy_pass http://app:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_redirect off;
    }
}

server {
    listen 80;
    server_name 13.61.100.110;

    return 301 https://keremboylu.com.tr$request_uri;
}

server {
    listen       80;
    server_name  keremboylu.com.tr www.keremboylu.com.tr;
    return 301 https://$host$request_uri;
}

server {
    listen       443 ssl;
    server_name  keremboylu.com.tr www.keremboylu.com.tr;
    server_tokens off;
    client_max_body_size 15M;
    ssl_certificate /etc/nginx/ssl/cert.pem;
    ssl_certificate_key /etc/nginx/ssl/key.pem;

    location / {
        proxy_pass http://app:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_redirect off;
    }
}

server {
    listen       80;
    server_name  admin.kensens.com.tr;
    client_max_body_size 50M;

    location / {
        proxy_pass http://kensens;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_redirect off;
    }
}

server {
    listen       80;
    server_name  admin.localhost;
    client_max_body_size 50M;

    location / {
        proxy_pass http://kensens;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_redirect off;
    }
}
